name: Playwright CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test-and-report:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm ci   # cleaner than npm install

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Run Playwright tests
        run: npx playwright test

      - name: Generate Allure report
        run: npm run report   # make sure package.json has "report": "allure generate allure-results --clean -o allure-report"

      - name: Setup Git identity
        run: |
          git config --global user.email "radixdt.2358@gmail.com"
          git config --global user.name "rxdt2358"

      - name: Deploy Allure report to GitHub Pages
        run: npx gh-pages -d allure-report -r https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/rxdt2358/playwright-allure-reports.git -u "github-actions-bot <github-actions[bot]@users.noreply.github.com>"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Send email with report
        run: npm run send-report
        env:
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
